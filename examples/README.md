# Example Workspaces

These folders are **semantic fixtures**, not Golden Sample parity examples.

They test Document Profile mapping, schema validation, Content Freeze gates,
component rendering, navigation, glossary behavior, responsive behavior, and
packaging. Their content and page counts are intentionally different from the
approved AFTERSHOCK Golden Sample.

Pre-generated HTML output is not stored here. Run the pipeline to create fresh
output from the fixture source.
