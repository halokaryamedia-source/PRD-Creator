# Next Action

Updated: 2026-08-11

## Current Status

`PRD_FLOW2_PROBLEM_SOLVING_AND_HUMANIZED_DECISIONS_STRENGTHENED`

Working branch: **`Local` only**.

## Current PRD contract

- Golden Sample remains the required hierarchy, page-composition, component-language, and presentation authority.
- Normal PRD creation/revision is Production Execution; no `development-brief`.
- Flow 2 is the production-recovery/problem-solving stage, not only source extraction/provenance.
- Flow 3 must not invent material topology, lifecycle behavior, numeric corrections, cross-role requirements, exclusions, terminology, or product decisions that Flow 2 should have recovered/resolved.
- Flow 4 keeps one-read multi-lens review plus mechanical validation and truthful visual evidence.

## Flow 2 recovery + problem-solving contract

```text
source inventory + inspection
→ explicit facts/rules/exclusions
→ topology + terminology
→ cross-role implications
→ production coverage
→ mechanic lifecycle + quantitative coherence
→ problem framing
→ Resolution Ladder
→ impact propagation
→ humanized grouped decision package only if needed
→ ready_for_prd
```

Key rules:

- material user instructions are persisted as source authority even when no file exists;
- source inspection coverage can be recorded as targeted/full for resumability;
- source-level supersession applies only when the whole source is replaced;
- removals/exclusions are first-class requirements;
- topology includes package order, global ownership, dependencies/transitions, and final result when relevant;
- material mechanics are checked for necessary Gameplay / Level Design / Developer / result-reset implications;
- lifecycle scan checks applicable precondition/trigger/active/success/fail/result/reset behavior without forcing irrelevant stages;
- directly related duration/count/capacity/scoring facts are checked for contradiction before drafting;
- each material issue is framed around observed problem, consequence, constraints, intended outcome, and affected roles;
- solve-before-ask ladder is authority → safe Completion → responsible recommended Proposal → Blocked/direct decision;
- one approved/recovered resolution is propagated to all actually affected requirements;
- related issues are grouped when one root decision resolves them;
- missing optional/irrelevant detail stays open/neutral rather than becoming filler or a user question.

## Humanized Flow 2 communication

User-facing decision/recovery explanations use clear production language:

```text
Masalah
Saran
Kenapa
Dampak
Alternatif — only when meaningful
```

This is a bounded Humanize presentation behavior, **not a new root skill or authority layer**. It may improve wording/order and hide internal jargon, but must not alter official terminology, numbers, timings, formulas, mechanics, triggers, uncertainty, provenance, or approval status.

Default to one recommendation; add at most 1–2 alternatives only for meaningful tradeoffs. Do not expose `SRC/REQ`, YAML, recovery-class jargon, or internal state unless requested or needed to explain a blocker.

## Efficiency boundary

Flow 2 improvement must not become user ceremony or a new artifact framework:

- no topology file;
- no coverage/lifecycle report file;
- no quantitative report file;
- no dependency graph;
- no mandatory checklist filled for every project;
- no new Humanize root skill;
- no source indexer/RAG/vector store;
- no fake file for chat instructions;
- no rereading unchanged source during bounded revisions;
- no question for optional/irrelevant detail.

Topology, exclusions, terminology, lifecycle/implication meaning, and solution impact remain normal requirement state. Humanized packages are user-facing views of existing recovery/decision meaning.

## Mechanical intake guard

No new Flow 2 validator was added. The existing validator is a Flow 4 owner and requires canonical/rendered artifacts; expanding it backward or creating another validator would add lifecycle/tooling complexity without a demonstrated state-contradiction failure. Add a small intake sanity guard only if real project evidence proves it is needed.

## Existing context/HTML efficiency

- mandatory boot remains compact and progressive;
- load only the smallest active Flow owner;
- renderer/validator consume large HTML at runtime rather than model context;
- canonical content is completed before the main projection;
- bounded revisions patch only affected scope;
- internal state is sparse but non-default conflicts/approvals/blockers/supersession/readiness remain explicit.

## Evidence boundary

This batch changes Flow 2 semantic/procedure/user-communication contracts only. It does not change Golden HTML, renderer behavior, Flow 4 validator mechanics, `content.md → render-data.json`, handoff semantics, or Voice behavior.

No real-project sample has yet exercised the full strengthened Flow 2 against genuinely incomplete source. CI proves repository/procedure consistency, not future design/recovery judgment quality.

## Next Step

Run **one real project with genuinely incomplete/uneven source through Flow 2 → 4**. Evaluate whether Flow 2 identifies the real issue, checks lifecycle/numeric coherence, produces responsible recommendations, propagates impact correctly, and presents decisions clearly enough that the user can approve/override them with minimal effort.
